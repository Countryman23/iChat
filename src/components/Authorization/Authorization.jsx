import React from 'react';
import AuthorizationItem from './AuthorizationItem';

const Authorization = () => {
    return (
        <div>
            <AuthorizationItem />
        </div>
    );
};

export default Authorization;
